= content_for :title, 'Pricing'
- content_for :current_menu, 'pricing'
.container
  .pricing-wrapper
    = render 'home/pricing',selected_plan: @selected_plan,disable_free: @disable_free
  .clearfix
  %br
  - if @show_checkout_dialog
    = render 'shared/pop_up_adding',selected_plan: @selected_plan