= form_tag add_tipster_cart_path do
  = hidden_field_tag :id, tipster_id
  - if defined? step
    = hidden_field_tag :step,'choose_offer'
  - disabled = false
  - if defined? disable
    - disabled = disable
  %button.btn.btn-sm{type: :submit, class: "#{disabled ? 'btn-default disabled' : 'btn-success'}"}
    %i.glyphicon.glyphicon-plus.mrs
    Subscribe