.container{'data-no-turbolink' => true}
  .navbar
    .navbar-inner
      .container
        %ul.nav.nav-pills
          %li
            %a{"data-toggle" => "tab", href: "#tab1"} Choose Offer
          %li.active
            %a{"data-toggle" => "tab", href: "#tab2"} Identification
          %li
            %a{"data-toggle" => "tab", href: "#tab3"} Payment Method
          %li
            %a{"data-toggle" => "tab", href: "#tab4"} Payment
          %li
            %a{"data-toggle" => "tab", href: "#tab5"} Confirmation
  %hr
  .tab-content
    .row.clearfix
      .col-md-5
        .text-center
          %h2 I'm returning user
        = simple_form_for :account, :url => subscribe_identification_path, :method => 'POST', :html => {class: 'form-horizontal'} do |f|
          = hidden_field_tag :act, 'sign_in'
          = f.input :email, :required => false, :autofocus => true, class: 'form-group',input_html: { :autocomplete => 'off'}
          = f.input :password, :required => false,class: 'form-group'
          .form-group
            .col-sm-offset-3.col-sm-9
              = f.button :submit, "Sign in", class: 'btn btn-success'
        %hr
        .text-center
          %h2 Connect from social network
          %ul.inline
            %li
              = simple_form_for :nothing, url: subscribe_identification_path do |f|
                = hidden_field_tag :act, 'facebook'
                %button.btn.btn-small.btn-primary{type: :submit} Facebook
            %li
              = simple_form_for :nothing, url: subscribe_identification_path  do |f|
                = hidden_field_tag :act, 'google_oauth2'
                %button.btn.btn-small.btn-warning{type: :submit} Google +
      .col-md-7
        .text-center
          %h2 I'm a New customer
        - subscriber = @subscriber || Subscriber.new
        = simple_form_for subscriber, url: subscribe_identification_path,:html => {class: 'form-horizontal'} do |f|
          = hidden_field_tag :act, 'sign_up'
          = render 'shared/subscribe_fields', f: f
          = f.simple_fields_for :account do |fr|

            = render 'shared/account_fields', f: fr
          .form-group
            .col-sm-offset-4.col-sm-7
              = f.button :submit, "Sign up", class: 'btn btn-primary'