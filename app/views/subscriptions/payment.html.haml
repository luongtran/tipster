.row-fluid
  .well
    %h3 Your select plan #{@plan.title}
    .row
      %h3
        Your selected tipster
      - @tipsters.each do |tipster|
        %p
          = tipster.name
    = link_to "Check out",init_payment_path,class: 'btn btn-warning',method: :post